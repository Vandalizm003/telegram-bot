return {
    -- Налаштування безпеки
    security = {
        -- Мінімальна довжина логіна
        minUsernameLength = 4,
        -- Мінімальна довжина паролю
        minPasswordLength = 6,
        -- Максимальна кількість спроб входу (анти-брутфорс)
        maxLoginAttempts = 5,
        -- Час блокування після перевищення спроб (в секундах)
        lockoutTime = 300, -- 5 хвилин
        -- Кількість раундів bcrypt (більше = безпечніше, але повільніше)
        bcryptRounds = 10
    },
    -- Налаштування відновлення паролю
    recovery = {
        -- Час дії коду відновлення (в хвилинах)
        codeExpirationMinutes = 15,
        -- Довжина коду
        codeLength = 6
    },
    -- UI налаштування
    ui = {
        -- Назва серверу для відображення в UI
        serverName = "My Server"
    }
}